var brokenWord = require("./brokenWord.js");

var newWord = function(word) {
    var letters = this.word.split("");
    for (var i = 0; i < letters.length; i++)
    {
        var currentWord = new brokenWord(letter[i]);
    }
}